@page
@model ApproveModel
@{
    ViewData["Title"] = Localizer["Approve Data Source"];
    ViewData["ActiveMainPage"] = "DataSource";
    ViewData["Level1"] = "Admin";
    var breadcrumbs = new[] {
        new { Title = "DataSource List", Url = "/ODMS/DataSource/Index", IsActive = false },
        new { Title = "Approve DataSource", Url = "", IsActive = true },
    };
}
<partial name="_BreadcrumbPartial" model=@(breadcrumbs) />
<form id="dataSource-form" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <partial name="_ApprovalStatusPartial" model="@Model.ApprovalStatus" />
    <div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.DataSource" />
    </div>
    <div id="toolbar-container" class="btn-group" role="group">
		<a asp-page="Index" class="btn btnFills btn-outline-secondary btnBackIcon" aria-label="@Localizer["Back"]" title="@Localizer["Back"]"></a>
		@if (Model.ApprovalStatus == FBSC.ODMS.Core.ODMS.ApprovalStatus.ForApproval
			|| Model.ApprovalStatus == FBSC.ODMS.Core.ODMS.ApprovalStatus.New)
		{
			<button type="button" id="btnApproveConfirmationModal" class="btn btnFills btn-outline-success btnApproveIcon cancel" name="Handler" value="Approve" aria-label="@Localizer["Approve"]" title="@Localizer["Approve"]"></button>
		}
		else
		{
			<button type="button" class="btn btnFills btn-outline-secondary btnApproveIcon" aria-label="@Localizer["Approve"]" title="@Localizer["Approve"]" disabled></button>
		}
		@if (Model.ApprovalStatus == FBSC.ODMS.Core.ODMS.ApprovalStatus.ForApproval
			|| Model.ApprovalStatus == FBSC.ODMS.Core.ODMS.ApprovalStatus.New)
		{
			<button type="button" id="btnRejectConfirmationModal" class="btn btnFills btn-outline-danger btnRejectIcon" name="Handler" value="Reject" aria-label="@Localizer["Reject"]" title="@Localizer["Reject"]"></button>
		}
		else
		{
			<button type="button" class="btn btnFills btn-outline-secondary btnRejectIcon" aria-label="@Localizer["Reject"]" title="@Localizer["Reject"]" disabled></button>
		}
	</div>
</form>
@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <partial name="_DisableFieldsPartial" model="@("PartialLoaderContainer")" />
    <script asp-add-nonce>
        $(document).ready(function () {
            function InitializeForm() {
                $.bindSaveWithRemarksConfirmationModal('Approve', '#btnApproveConfirmationModal', '#dataSource-form', 'ApprovalRemarks');
                $.bindSaveWithRemarksConfirmationModal('Reject', '#btnRejectConfirmationModal', '#dataSource-form', 'ApprovalRemarks');
            }
            InitializeForm();
        });
    </script>
}