@using Models
@model ProjectViewModel

<div class="card">
    <div class="card-body">
        <input type="hidden" asp-for="@Model.Id" name="Id" />
        <input type="hidden" asp-for="@Model.SOWFileName" />
        <partial name="_EditProjectDetails" model="Model" />
    </div>
</div>

<br />
<div class="card">
    <div class="card-body">
        <h6 class="form-section-title fieldrequired" id="sowSectionTitle">@Localizer["Statement of Work"]</h6>
        <p class="sow-hint">@Localizer["Attach the required document"]</p>
        <div class="sow-dropzone" id="sowDropzone">
            <input type="file" id="SOWForm" name="SOWForm" accept=".pdf,.docx,.jpg,.jpeg,.png,application/pdf,application/vnd.openxmlformats-officedocument.wordprocessingml.document,image/png,image/jpeg" class="sow-dropzone-input" />
            <span class="sow-dropzone-icon"><i class="fas fa-upload" aria-hidden="true"></i></span>
            <span class="sow-dropzone-text"><span class="sow-dropzone-link">@Localizer["Click to upload"]</span> @Localizer["or drag and drop"]</span>
            <span class="sow-dropzone-types">PDF, DOCX, JPG, PNG, XLSX</span>
            <span class="sow-dropzone-filename" id="sowSelectedFile">@(string.IsNullOrEmpty(Model.SOWFileName) ? "" : System.IO.Path.GetFileName(Model.SOWFileName))</span>
        </div>
        <span asp-validation-for="@Model.SOWForm" class="text-danger d-block mt-1"></span>
        <div class="form-check mt-2">
            <input class="form-check-input" type="checkbox" asp-for="@Model.NoSOW" />
            <label class="form-check-label" asp-for="@Model.NoSOW">@Localizer["No SOW yet"]</label>
        </div>
    </div>
</div>

<br /><partial name="_TeamMembersList" model="Model" />
