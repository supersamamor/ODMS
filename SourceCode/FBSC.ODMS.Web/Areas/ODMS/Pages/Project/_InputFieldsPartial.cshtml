@using Models
@model ProjectViewModel

@* No card-header here - the first form-section title ("Project Information")
   is the card's visible heading, matching the reference design. *@
<div class="card">
    <div class="card-body">
        <input type="hidden" asp-for="@Model.Id" name="Id" />
        <partial name="_EditProjectDetails" model="Model" />        
    </div>
</div>

<br /><partial name="_TeamMembersList" model="Model" />