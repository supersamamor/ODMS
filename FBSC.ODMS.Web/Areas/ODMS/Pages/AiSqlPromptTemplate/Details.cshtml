@page
@model DetailsModel
@{
    ViewData["Title"] = Localizer["View AI SQL Prompt Template"];
    ViewData["ActiveMainPage"] = "AiSqlPromptTemplate";
    ViewData["Level1"] = "ODMS";
	var breadcrumbs = new[] {
		new { Title = "AI SQL Prompt Template List", Url = "/ODMS/AiSqlPromptTemplate/Index", IsActive = false },
		new { Title = "View AI SQL Prompt Template", Url = "", IsActive = true },
	};
}
<partial name="_BreadcrumbPartial" model=@(breadcrumbs) />
<div id="aiSqlPromptTemplate-container">
    <input type="hidden" asp-for="@Model.RemoveSubDetailId" />
    <input type="hidden" asp-for="@Model.AsyncAction" />
    <div id="PartialLoaderContainer">
        <partial name="_InputFieldsPartial" model="Model.AiSqlPromptTemplate" />
    </div>
</div>
<div id="toolbar-container" class="btn-group" role="group">
    <a asp-page="Index" class="btn btnFills btn-outline-secondary btnBackIcon" title="@Localizer["Back"]"></a>
    @if ((await AuthorizationService.AuthorizeAsync(User, Permission.AiSqlPromptTemplate.Create)).Succeeded)
    {
        <a asp-page="Add" id="btnAddRecord" class="btn btnFills btn-outline-success btnAddIcon" title="@Localizer["Add"]"></a>
    }
    @if ((await AuthorizationService.AuthorizeAsync(User, Permission.AiSqlPromptTemplate.Edit)).Succeeded)
    {
        <a asp-page="Edit" asp-route-id="@Model.AiSqlPromptTemplate.Id" class="btn btnFills btn-outline-primary btnEditLightIcon" title="@Localizer["Edit"]"></a>
    }
    @if ((await AuthorizationService.AuthorizeAsync(User, Permission.AiSqlPromptTemplate.Delete)).Succeeded)
    {
        <a asp-page="Delete" asp-route-id="@Model.AiSqlPromptTemplate.Id" class="btn btnFills btn-outline-danger btnDeleteIcon" title="@Localizer["Delete"]"></a>
    }
	@if ((await AuthorizationService.AuthorizeAsync(User, Permission.AiSqlPromptTemplate.History)).Succeeded)
	{
        <a asp-page="History" asp-route-id="@Model.AiSqlPromptTemplate.Id" class="btn btnFills btn-outline-primary btnHistoryIcon" title="@Localizer["History"]"></a>
	}
</div>

@section Scripts {
<partial name="_DisableFieldsPartial" model="@("aiSqlPromptTemplate-container")" />
}